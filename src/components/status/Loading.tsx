import "../../styles/status.css";

// loader of words scrumbled and reorganized aggain

function Loading() {
  return (
    <div className="loader-container">
      <span className="loader"></span>
      <span>looking up ...</span>
    </div>
  );
}

export default Loading;
